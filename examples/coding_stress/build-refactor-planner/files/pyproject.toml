[project]
name = "flowforge"
version = "0.1.0"
description = "Dependency-free milestone planning fixture."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
