# Legacy Notes

The old spreadsheet importer was removed. Do not add CSV, YAML, or third-party
parsing dependencies for this fixture. The harness should force the model to
work with the existing pipe-delimited format and standard-library code.
